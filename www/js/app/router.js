define([
    'core/AppRouter',
], function (AppRouter) {
    'use strict';

    var Router = AppRouter.extend({
        uses: [
        ],

        customRoutes: {
        },
    });

    return Router;

});
